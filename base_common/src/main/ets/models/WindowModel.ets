@ObservedV2
export class WindowModel {
  @Trace windowTopPadding: number = 0;
  @Trace windowBottomPadding: number = 0;
  @Trace windowWidth: number = 0;
  @Trace windowHeight: number = 0;
  @Trace isFloatWindow: boolean = false;
  @Trace showNavigation: boolean = false;
}